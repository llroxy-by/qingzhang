{
  "name": "server",
  "version": "1.0.0",
  "description": "零依赖：仅使用 Node.js 内置模块（`http` + `node:sqlite`），不需要 `npm install`。",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "imapflow": "^2.0.0"
  }
}
